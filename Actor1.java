package Weekend_Test;

public class Actor1 {
	{
		/*String actor_Name;int actor_Id;
		Actor1(){}
		Actor1(String actor_Name,int actor_Id)
		{
			this.actor_Name = actor_Name;
			this.actor_Id = actor_Id;
		}
		public String toString() {
			return "Name of the Actor " + actor_Name +" Actor Id " +actor_Id;
		}
		public int hashCode()
		{
			int ans = actor_Name.hashCode() + actor_Id * 2;
			return ans;
		}

		public boolean equals(Object obj) {	
			if(obj == null)
				return false;
			if(this == obj)
				return true;
			if(this.getClass() != obj.getClass())
				return false;
			Actor a1 = (Actor)obj;
			if(this.actor_Name == a1.actor_Name && this.actor_Id == a1.actor_Id)
			{
				return true;
			}
			else
			{
				return false;
			}	
		}


	}
	class Movie
	{
		String name;String producer;double budget;Actor ar;
		Movie(){}
		Movie(String name,String producer,double budget,Actor ar)
		{
			this.name = name;
			this.producer = producer;
			this.budget = budget;
			this.ar = ar;
		}
		public String toString() {
			return "Movie name :"+ name + "\nProducer name : " + producer + "\nBudget of Movie : " + budget + "\nActor : " + ar;
		}
		@Override
		public int hashCode() 
		{
			int ans = name.hashCode() + producer.hashCode() + (int)budget + ar.hashCode();
			return ans;

		}
		public boolean equals(Object obj)
		{
			if(obj == null)
				return false;
			if(this == obj)
				return true;
			if(this.getClass() != obj.getClass())
				return false;
			Movie m = (Movie)obj;
			if(this.name == m.name && this.producer == m.producer && this.budget == m.budget && this.ar == m.ar)
				return true;
			else
				return false;
		}
*/	}


}
